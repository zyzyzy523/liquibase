#Expected Output:
#ChangeLog{items=[Preconditions{items=[DBMSPrecondition{type=mysql}, TableExistsPrecondition{catalogName=my_cat, tableName=test_table}], onError=HALT, onFail=HALT, onSqlOutput=IGNORE}], physicalPath=simple_precondition.yaml}

databaseChangeLog:
  - preconditions:
    - dbms:
        type: mysql
    - tableExists:
        catalogName: my_cat
        tableName: test_table
